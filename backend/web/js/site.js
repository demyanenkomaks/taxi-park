$('.mask-phone').mask('+0 (000) 000-00-00'); // Телефон
$('.mask-cart-bank').mask('0000 0000 0000 0000'); // Банковская карта
$('.mask-prava-num').mask('00 00 000000'); // Права номер
$('.mask-year').mask('0000'); // Год